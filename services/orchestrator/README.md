# Orchestrator

Later service for the benchmark lifecycle state machine:

```text
QUEUED -> BUILDING -> SANDBOX_STARTING -> HEALTHCHECKING -> BENCHMARKING -> VALIDATING -> SCORED -> FINISHED
```

Not part of the first vertical slice.

